# Category-first discovery trial

Status: preregistered, not a result. Prepared 2026-08-30 on Windows/Node 22.21.0. Core source commit at registration: `03e9efa`; site commit: `1656307`. Neither checkout is supplied as candidate input. The trial evaluates the public information environment, not unpublished local improvements.

The exact agent prompt is `discovery-prompt.txt`. A fresh agent receives that prompt without conversation history and must begin with three package-agnostic searches, freezing its shortlist before package-specific investigation. This differs from the earlier trial's package-directed initial searches. No package is nominated. The product brief is the same counter/chat/presence problem used in the assigned-use protocol in `docs/AGENT_EVALUATION.md`.

The root agent will preserve the final report verbatim, record the available agent identity and observation timestamps, and independently check whether the selected stack can plausibly satisfy the brief from its primary documentation. A report of selection is not an implementation pass. Search listings do not establish complete market coverage, an agent's self-reported process is not complete tool telemetry, and one convenience sample does not establish a discovery rate. The host workspace can expose a project name even without conversation history; any exposure must be disclosed, not described as full blinding.

No repairs, package hints, candidate source, implementation guidance or feedback are provided before the choice is frozen. Current guide publication remains a separate open requirement. No historical trial artifacts are modified.
