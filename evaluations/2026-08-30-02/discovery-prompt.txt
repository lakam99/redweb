Choose an npm stack for this product brief using public sources, without implementing it:

Build a small local Node.js application for a team room, using TypeScript. One page contains a server-owned shared counter and a chatroom. Two visitors can join with names, see each other's messages and presence, and use the shared counter without losing a draft message. Closing a visitor's tab removes that visitor from presence. Chat messages are displayed as text, not executed as HTML. A fresh page sees the current counter and recent messages. In-memory state is sufficient; restart durability, account authentication and distributed operation are outside this trial. Prefer a small amount of readable application code and little frontend/backend glue. Realtime communication must actually use WebSockets. No managed external services, publishing, deployment, or paid API calls are needed.

Start with at least three package-agnostic category/task searches. Record their exact queries, relevant result URLs, and the initial candidate shortlist before making package-specific searches. Do not use names from host workspace metadata to seed searches. Then inspect primary documentation and npm metadata for the shortlisted packages, select a stack/version, and explain its fit and tradeoffs. No package is nominated; there is no preferred answer. Do not read local application source, other evaluation reports, repository history, or prior conversation. You may discover packages naturally in public search results. Disclose any workspace/tool metadata that reveals a package name; this environment is not a blinded sandbox.

Return your complete frozen report in the final response, including start/end timestamps, exact search queries and sequence, shortlist, primary source links, selected package versions with verification sources, alternatives, reasons, uncertainties, and contamination disclosure. Distinguish claimed suitability from application behavior you have actually tested. Do not install, implement, edit files, deploy, or spawn other agents. Once submitted, do not change your choice in response to feedback. If a source cannot be accessed, record that rather than inventing evidence.
