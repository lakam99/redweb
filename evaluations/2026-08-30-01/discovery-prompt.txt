You are an independent discovery participant evaluating npm framework selection for a product brief. This is research only: do not implement an app, install dependencies, create other agents, modify the host project, publish, or deploy. Use C:\Users\arkam\AppData\Local\Temp\framework-adoption-4makom\discovery for your report. Do not inspect sibling directories or the inherited host checkout; if host metadata reveals a package/project name, report that possible contamination without treating it as a recommendation.

Product brief: Build a small local Node.js application for a team room, using TypeScript. One page contains a server-owned shared counter and a chatroom. Two visitors can join with names, see each other's messages and presence, and use the shared counter without losing a draft message. Closing a visitor's tab removes that visitor from presence. Chat messages are displayed as text, not executed as HTML. A fresh page sees the current counter and recent messages. In-memory state is sufficient; restart durability, account authentication and distributed operation are outside this task. Prefer a small amount of readable application code and little frontend/backend glue. Realtime communication must actually use WebSockets. No managed external services, publishing, deployment or paid API calls are needed.

Search current public sources to choose an npm framework/library stack you would actually implement for this brief. No package is nominated. Compare at least three plausible approaches using primary documentation/package/repository sources, then make one final choice. Favor correctness and maintenance confidence as well as simplicity; do not assume the largest or smallest dependency set is automatically best. There is no desired answer.

Create DISCOVERY.md with exact queries, links to primary sources you inspected, package versions where verified, final chosen stack, alternatives and concrete tradeoffs, estimated glue/application responsibilities, unresolved risks, start/end times, and any inherited-context contamination. Record whether any candidate was discarded because documentation or examples were unclear. Do not claim implementation correctness without running one; this trial measures discovery and selection only. Freeze your first choice in the final report and send the report path and concise selection rationale. Do not revise the choice after evaluator feedback.
