You are an independent implementation participant in a preregistered adoption trial. Work only in C:\Users\arkam\AppData\Local\Temp\framework-adoption-4makom\assigned. Do not modify the host project, create other agents, publish, deploy, or inspect the host repository/tests. No conversation history is needed.

Use the nominated Redweb candidate archive C:\Users\arkam\AppData\Local\Temp\framework-adoption-4makom\artifacts\redweb-0.12.0.tgz. Its public documentation is in C:\Users\arkam\AppData\Local\Temp\framework-adoption-4makom\artifacts\package: README.md, docs, recipes, examples and public .d.ts files may be read. Do not inspect the package's implementation JS/src or host implementation. Install the archive into your own app with npm; do not substitute published redweb. This is an unreleased candidate despite package metadata 0.12.0. You may use normal npm dependencies and their public documentation. Windows Node is 22.21.0; if registry certificate verification needs the Windows trust store, use NODE_OPTIONS=--use-system-ca, never disable TLS verification.

Build a small local TypeScript application for a team room using Redweb's documented decorator authoring model. One page contains a server-owned shared counter and chatroom. Two visitors join with names, see each other's messages and presence, and use the shared counter without losing a draft message. Closing a visitor's tab removes that visitor from presence within five seconds. Chat messages display literal text, not executable HTML. A fresh page sees the current counter and recent messages. In-memory state is sufficient; no account authentication, restart durability or distributed operation is required. Prefer small readable code and little frontend/backend glue. Actual WebSockets must carry realtime traffic.

Observable contract: npm run build produces dist/app.js; npm test runs your own actual-network test without mocks. PORT=0 node dist/app.js binds only 127.0.0.1 and prints a JSON line {"url":"http://127.0.0.1:<actual-port>"}. GET / serves the app. Use these data-testid values: count (decimal counter initially zero), increment (button), name (input), join (button), message (input or textarea), send (button), messages (container), members (container). Alice and Bob joining must appear in both member containers. Sending a message must show sender and text in both message containers. Counter clicks from either tab update both. Joining may hide the name/join controls. No need for visual polish or deployment.

Record your start/end times, commands and outcomes, local build/test failures and repairs, and files/docs consulted in TRIAL_LOG.md. These logs are self-reported telemetry, not independent proof. You can repair your app locally before first submission, but preserve the record of failed attempts. When satisfied, stop all processes you started and send the final app path, commands, known limitations, and summary. Do not edit your first submission after reporting it unless given a numbered repair request. An independent evaluator will test the submitted app without reading your tests. If the documentation leaves an obstacle, record it rather than asking the evaluator for implementation advice.
